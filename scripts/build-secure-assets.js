const fs = require("fs"); const path = require("path"); const JavaScriptObfuscator = require("javascript-obfuscator"); const mainContent = fs.readFileSync("main.js", "utf8"); const obfuscated = JavaScriptObfuscator.obfuscate(mainContent, { compact: true, controlFlowFlattening: true }); if (!fs.existsSync("dist")) fs.mkdirSync("dist"); fs.writeFileSync("dist/main.min.js", obfuscated.getObfuscatedCode()); console.log("Assets seguros gerados com sucesso!");